{
  "framework": "vite",
  "buildCommand": "pnpm build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/post/:postId", "destination": "/api/post/:postId" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
